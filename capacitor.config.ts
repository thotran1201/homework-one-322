import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'homework-one-322',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
